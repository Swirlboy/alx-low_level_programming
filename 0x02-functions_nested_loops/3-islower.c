#include "main.h"

/**
 * main - check code
 *
 * _islower - check for lowercase characters
 * @c : character to print
 * return: 1 if c is lowercase else return: 0
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
